#include <stdio.h>

int main()
{

    int a, b;

    printf("enter a number:");
    scanf("\n %d", &a);

    printf("enter b number:");
    scanf("\n %d", &b);

    printf("\naddition is %d", a + b);

    printf("\n subtraction is %d", a - b);

    printf("\n multiplication  is %d", a * b);

    printf("\ndivision is %d", a / b);

    printf("\nmodule is %d", a % b);

    return 0;
}